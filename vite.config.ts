// vite.config.js
import { defineConfig } from 'vite';

export default defineConfig({
  // This is the only configuration you need for GitHub Pages.
  // It must match the name of your repository.
  base: '/aivoicebishal/',
});
